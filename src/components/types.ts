export type infoType = {
  title: string;
  img: any;
  description: string;
  tag?: string[];
};

export type teamMember = {
  id: number;
  name: string;
  position: string;
  photo: any;
};

export type careerFeature = {
  id: number;
  name: string;
  description: string;
};
